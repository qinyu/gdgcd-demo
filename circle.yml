machine:
  java:
    version: jdk1.8.0

dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter tools-preview,platform-tools-preview,build-tools-24.0.0-preview,android-N,extra-android-support,extra-android-m2repository

test:
  override:
    - ./gradlew assembleProductDebug
